#include <math.h>
#include <stdio.h>

int main{

    double val = 0.0;

    for(i = 2; i < 1000000000; i++){
        val = val + 3*x/((x*x-2)*log(x))
    }

    printf("val = %g\n", val);

    return 0;
}